{
  "engines": {
    "node": ">=26.0.0"
  },
  "scripts": {
    "test": "npm test --prefix wrongsecrets-balancer && npm test --prefix cleaner",
    "test-cypress": "K8S_ENV=mock CYPRESS_ADMIN_PASSWORD=$(kubectl get secrets wrongsecrets-balancer-secret -o=jsonpath='{.data.adminPassword}' | base64 --decode || echo 'admin') npm run test-cypress --prefix wrongsecrets-balancer"
  },
  "devDependencies": {
    "@commitlint/config-conventional": "^20.0.0"
  },
  "dependencies": {
    "eslint": "^10.0.0"
  }
}
